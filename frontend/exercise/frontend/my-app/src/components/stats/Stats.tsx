import React, {FC} from "react";

const Stats: FC = () => {
    //todo expenses and categories
    //todo use chartjs to draw chart of expenses
    return (
        <div>

        </div>
    );
};

export default Stats;