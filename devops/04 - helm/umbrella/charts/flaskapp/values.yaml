replicaCount: 1

image:
  repository: accuratiorem/flaskapptest
  pullPolicy: IfNotPresent
  tag: "latest"

## Flaskapp values
flaskapp:
  apikey: NTI1ZjE2YmUtNTRlMS00ZjdjLTg3M2YtZDQ4OWU4Mjg1ZjA2LWRldgo=
## Postgresql values
postgresql:
  username: postgres
  password: c2VjdXJlX1BAc3N3b3JkX2Rldgo=
  database: flaskapp_db
  import:
    command: "sleep 12 && python -c 'import database; database.init_db()'"

serviceAccount:
  create: true
  name: "artur-app"

service:
  type: NodePort
  port: 80

container:
  port: 5090

ingress:
  enabled: true
  annotations:
      kubernetes.io/ingress.class: nginx
  hosts:
    - host: flaskapp
      paths:
      - /

  tls: []

resources:
    limits:
      cpu: 100m
      memory: 128Mi
    requests:
      cpu: 100m
      memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}
